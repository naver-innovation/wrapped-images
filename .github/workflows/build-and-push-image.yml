name: 'build-and-push-image'

# main 브랜치에 <repo>/<tag>/Dockerfile 가 push 되면
# 해당 이미지를 amd64 / arm64 네이티브로 빌드한 뒤 multi-arch manifest 로 OCIR 에 push 한다.
#
# 디렉토리 규칙 (repo 루트 = OCIR repository 경로):
#   quay.io/kiwigrid/k8s-sidecar/2.5.0/Dockerfile
#     -> me-riyadh-1.ocir.io/axlo4g31gl45/quay.io/kiwigrid/k8s-sidecar:2.5.0
#   docker.io/library/busybox/1.37.0/Dockerfile
#     -> me-riyadh-1.ocir.io/axlo4g31gl45/docker.io/library/busybox:1.37.0
#   (Dockerfile 의 상위 디렉토리명 = tag, 그 위 전체 경로 = OCIR repository)

on:
  push:
    branches:
      - main
    paths:
      - '**/Dockerfile'
  workflow_dispatch:
    inputs:
      image_dir:
        description: '재빌드할 이미지 경로 (예: quay.io/kiwigrid/k8s-sidecar/2.5.0). 비우면 전체 재빌드.'
        required: false
        default: ''

concurrency:
  # 각 push 는 "그 커밋이 바꾼 Dockerfile" 만 빌드(detect 의 git diff before..sha)하므로,
  # ref 단위로 묶으면 연속 머지 시 pending run 이 최신 run 에게 밀려 취소되어 빌드가 유실된다.
  # SHA 단위로 묶어 커밋끼리는 서로 취소하지 않고, 동일 커밋 재실행만 직렬화한다.
  group: ${{ github.workflow }}-${{ github.sha }}
  cancel-in-progress: false

env:
  REGISTRY: me-riyadh-1.ocir.io
  NAMESPACE: axlo4g31gl45
  OCI_REGION: me-riyadh-1

jobs:
  # 1) 변경된(또는 dispatch 로 지정된) Dockerfile 디렉토리를 찾아 빌드 매트릭스를 생성한다.
  detect:
    runs-on: arc-amd64
    outputs:
      build_matrix: ${{ steps.detect.outputs.build_matrix }}
      image_matrix: ${{ steps.detect.outputs.image_matrix }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Detect changed image directories
        id: detect
        run: |
          set -euo pipefail
          declare -A DIRS

          # 변경 파일이 속한 이미지 디렉토리(= Dockerfile 이 있는 가장 가까운 상위 디렉토리)를 찾는다.
          add_dir_from_file() {
            local f="$1" d
            d=$(dirname "$f")
            while [[ "$d" != "." && "$d" != "/" ]]; do
              if [[ -f "$d/Dockerfile" ]]; then DIRS["$d"]=1; return 0; fi
              d=$(dirname "$d")
            done
          }

          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            INPUT="${{ github.event.inputs.image_dir }}"
            INPUT="${INPUT#./}"; INPUT="${INPUT%/}"
            if [[ -n "$INPUT" ]]; then
              if [[ -f "$INPUT/Dockerfile" ]]; then
                DIRS["$INPUT"]=1
              else
                echo "::error::No Dockerfile at $INPUT"; exit 1
              fi
            else
              while IFS= read -r df; do d="${df#./}"; DIRS["$(dirname "$d")"]=1; done \
                < <(find . -type f -name Dockerfile -not -path './.git/*')
            fi
          else
            BEFORE="${{ github.event.before }}"
            if [[ -z "$BEFORE" || "$BEFORE" =~ ^0+$ ]]; then
              RANGE_BASE=$(git hash-object -t tree /dev/null)   # 빈 트리 (초기 push 대응)
            else
              RANGE_BASE="$BEFORE"
            fi
            while IFS= read -r f; do add_dir_from_file "$f"; done \
              < <(git diff --name-only "$RANGE_BASE" "${{ github.sha }}")
          fi

          # 매트릭스 JSON 생성 (jq 의존 없이 직접 구성)
          build_json="["; image_json="["; fb=1; fi=1
          for d in $(printf '%s\n' "${!DIRS[@]}" | sort); do
            tag="${d##*/}"; image="${d%/*}"
            [[ $fi -eq 1 ]] && fi=0 || image_json+=","
            image_json+="{\"dir\":\"$d\",\"image\":\"$image\",\"tag\":\"$tag\"}"
            for a in amd64 arm64; do
              [[ $fb -eq 1 ]] && fb=0 || build_json+=","
              build_json+="{\"dir\":\"$d\",\"image\":\"$image\",\"tag\":\"$tag\",\"arch\":\"$a\",\"runner\":\"arc-$a\"}"
            done
          done
          build_json+="]"; image_json+="]"

          echo "build_matrix=$build_json" >> "$GITHUB_OUTPUT"
          echo "image_matrix=$image_json" >> "$GITHUB_OUTPUT"
          if [[ ${#DIRS[@]} -eq 0 ]]; then
            echo "변경된 이미지 없음 — 빌드 skip"
          else
            echo "Detected images:"; printf '%s\n' "${!DIRS[@]}" | sort | sed 's/^/  - /'
          fi

  # 2) OCIR 은 이 tenancy 에서 첫 push 시 repository 자동 생성이 꺼져 있다.
  #    따라서 build/push 전에 OCI API 로 대상 repository 를 보장한다.
  ensure-repositories:
    needs: detect
    if: needs.detect.outputs.image_matrix != '[]'
    runs-on: arc-amd64
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.detect.outputs.image_matrix) }}
    env:
      OCI_TENANCY_OCID: ${{ secrets.OCI_TENANCY_OCID }}
      OCI_USER_OCID: ${{ secrets.OCI_USER_OCID }}
      OCI_FINGERPRINT: ${{ secrets.OCI_FINGERPRINT }}
      OCI_PRIVATE_KEY: ${{ secrets.OCI_PRIVATE_KEY }}
      OCI_COMPARTMENT_OCID: ${{ secrets.OCI_COMPARTMENT_OCID }}
    steps:
      - name: Validate OCI API credentials
        run: |
          set -euo pipefail
          missing=()
          for name in OCI_TENANCY_OCID OCI_USER_OCID OCI_FINGERPRINT OCI_PRIVATE_KEY OCI_COMPARTMENT_OCID; do
            if [[ -z "${!name:-}" ]]; then
              missing+=("$name")
            fi
          done
          if [[ ${#missing[@]} -gt 0 ]]; then
            printf '::error::Missing required secrets for OCIR repository pre-create: %s\n' "${missing[*]}"
            printf '::error::Set OCI API credentials with REPOSITORY_MANAGE permission, otherwise new OCIR repositories cannot be pushed because first-push auto-create is disabled.\n'
            exit 1
          fi

      - name: Install OCI CLI
        run: |
          set -euo pipefail
          if command -v oci >/dev/null 2>&1; then
            oci --version
            exit 0
          fi
          python3 -m pip install --user --upgrade oci-cli
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"

      - name: Configure OCI CLI
        run: |
          set -euo pipefail
          mkdir -p "$HOME/.oci"
          key_file="$RUNNER_TEMP/oci_api_key.pem"
          printf '%s\n' "$OCI_PRIVATE_KEY" > "$key_file"
          chmod 600 "$key_file"
          cat > "$HOME/.oci/config" <<EOF
          [DEFAULT]
          user=${OCI_USER_OCID}
          fingerprint=${OCI_FINGERPRINT}
          tenancy=${OCI_TENANCY_OCID}
          region=${OCI_REGION}
          key_file=${key_file}
          EOF
          chmod 600 "$HOME/.oci/config"

      - name: Ensure OCIR repository exists
        run: |
          set -euo pipefail
          repo="${{ matrix.image }}"
          echo "Ensuring OCIR repository: ${repo}"

          set +e
          output=$(oci artifacts container repository create \
            --compartment-id "$OCI_COMPARTMENT_OCID" \
            --display-name "$repo" \
            --is-public false \
            --output json 2>&1)
          rc=$?
          set -e

          printf '%s\n' "$output"
          if [[ $rc -eq 0 ]]; then
            exit 0
          fi
          if grep -Eiq '409|Conflict|AlreadyExists|already exists|exists already|has already been created' <<<"$output"; then
            echo "OCIR repository already exists: ${repo}"
            exit 0
          fi

          printf '::error::Failed to ensure OCIR repository %s in compartment %s.\n' "$repo" "$OCI_COMPARTMENT_OCID"
          printf '::error::The OCI principal must be able to create/manage Container Registry repositories before docker push can work.\n'
          exit "$rc"

  # 3) 아키텍처별 네이티브 러너에서 빌드 후 arch-suffix 태그로 push 한다.
  build:
    needs: [detect, ensure-repositories]
    if: needs.detect.outputs.build_matrix != '[]'
    runs-on: ${{ matrix.runner }}
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.detect.outputs.build_matrix) }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
        with:
          platforms: linux/${{ matrix.arch }}

      - name: Log in to Oracle Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.OCIR_USERNAME }}
          password: ${{ secrets.OCIR_AUTH_TOKEN }}

      - name: Build and push (${{ matrix.arch }})
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.dir }}
          file: ${{ matrix.dir }}/Dockerfile
          platforms: linux/${{ matrix.arch }}
          push: true
          tags: ${{ env.REGISTRY }}/${{ env.NAMESPACE }}/${{ matrix.image }}:${{ matrix.tag }}-${{ matrix.arch }}
          cache-from: type=gha,scope=${{ matrix.image }}-${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=${{ matrix.image }}-${{ matrix.arch }}
          provenance: false
          sbom: false

  # 4) arch 별 이미지를 묶어 multi-arch manifest(원본 태그)를 생성한다.
  manifest:
    needs: [detect, build]
    if: needs.detect.outputs.image_matrix != '[]'
    runs-on: arc-amd64
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(needs.detect.outputs.image_matrix) }}
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to Oracle Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.OCIR_USERNAME }}
          password: ${{ secrets.OCIR_AUTH_TOKEN }}

      - name: Create and push multi-arch manifest
        run: |
          set -euo pipefail
          IMG="${{ env.REGISTRY }}/${{ env.NAMESPACE }}/${{ matrix.image }}"
          TAG="${{ matrix.tag }}"
          docker buildx imagetools create \
            --tag "${IMG}:${TAG}" \
            "${IMG}:${TAG}-amd64" \
            "${IMG}:${TAG}-arm64"
          echo "Pushed multi-arch manifest: ${IMG}:${TAG} (amd64, arm64)"
          docker buildx imagetools inspect "${IMG}:${TAG}"
